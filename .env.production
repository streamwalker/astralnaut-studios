VITE_PAYMENTS_CLIENT_TOKEN="pk_live_51TbyK9KAmsXwKtWbaOHSVXp246MMTeod3GCUDpAoXR4YZw4RMT9upWxQjilyZf6X89SLQNUd17j9s6zqGJMZOZgZ00kCR5h8VX"

# Meta Pixel. Production only — absent from .env / .env.development so local
# and preview traffic never reaches the ad account's optimization data.
#
# This is the pixel the live ads reference and the one the pre-migration site
# already fired. Do NOT swap it without repointing every active ad set in Ads
# Manager first — changing it silently orphans attribution on running campaigns.
VITE_META_PIXEL_ID="2035981390457132"

# Hero background video. Exceeds the 25 MiB Cloudflare static-asset cap, so it
# is hosted in Supabase Storage rather than committed. Empty = hero falls back
# to the committed poster still. See src/lib/media.ts.
VITE_HERO_VIDEO_URL=
